// lib/pages/page_two.dart
import 'package:flutter/material.dart';

class fishDetectorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Fish Detector")),
      body: Center(child: Text("This is Page Two")),
    );
  }
}
